interface:
  display_name: "Prototype Lab"
  short_description: "Build, compare, and package portable UI labs."
  default_prompt: "Use $prototype-lab to create a self-contained browser prototype, preserve reusable prompts and run receipts, compare isolated variants when requested, verify it visually, and package it as a portable static ZIP pack."
