interface:
  display_name: "Prototype Lab"
  short_description: "Manage portable prototype workspaces and hubs."
  default_prompt: "Use $prototype-lab to create a standalone artifact, add a managed comparison hub when needed, sync workspace health, verify it, and package the ready result."
